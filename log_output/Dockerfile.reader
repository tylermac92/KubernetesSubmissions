# syntax=docker/dockerfile:1
FROM python:3.12-slim
WORKDIR /app
COPY log_output_reader.py /app/
RUN pip install --no-cache-dir fastapi uvicorn
ENV LOG_PATH=/data/log.txt
ENV PORT=8000
EXPOSE 8000
CMD ["sh", "-c", "uvicorn log_output_reader:app --host 0.0.0.0 --port ${PORT}"]

